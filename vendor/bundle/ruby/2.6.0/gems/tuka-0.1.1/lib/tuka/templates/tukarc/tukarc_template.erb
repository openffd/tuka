
# input your Bitbucket credentials
BITBUCKET_USERNAME=''
BITBUCKET_PASSWORD=''




# IMPORTANT: don't modify this!
shopt -s extglob
isv () { if [[ -z "${!1}" || "${!1}" =~ ^\ +$ ]]; then return 1; else return 0; fi }




















































































































































































































































































# optional
NEXTCLOUD_USERNAME=''
NEXTCLOUD_PASSWORD=''
GITLAB_USERNAME=''
GITLAB_PASSWORD=''
if isv BITBUCKET_USERNAME && isv BITBUCKET_PASSWORD; then
  export BITBUCKET_USERNAME;
  export BITBUCKET_PASSWORD;
else
  echo -e "\n[×] One of the required credentials you entered is invalid! Check the ~/.tukarc file";
  echo -e "\nRun 'tuka sc' to set this up again.";
fi
